const initialState = {
  name: "Daniel",
  lastName: "Meza Miranda",
  age: 25
};

export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
